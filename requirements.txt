# ============================================
# Crypto Signal Bot - Python Dependencies
# All indicators are implemented from scratch in src/indicators/
# (no pandas-ta / ta / stocktrend needed)
# ============================================

# --- Core / HTTP ---
requests==2.32.3
python-dotenv==1.0.1
pyyaml==6.0.2

# --- Data / Numeric ---
pandas==2.2.2
numpy>=1.26.4,<2.0.0

# --- Machine Learning ---
scikit-learn==1.5.0
joblib==1.4.2

# --- Telegram notifications ---
requests>=2.31.0

# --- Web Dashboard backend (FastAPI) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1

# --- Scheduling ---
APScheduler==3.10.4

# --- v5.3 WebSocket candle feed (zero REST weight for klines) ---
websocket-client==1.8.0

# --- Utilities ---
rich==13.7.1
tabulate==0.9.0
colorama==0.4.6

# --- Database (PostgreSQL support - Render free PostgreSQL) ---
# Optional: only loaded if DATABASE_URL env var is set
# SQLite is built-in to Python (no extra deps needed for SQLite)
psycopg2-binary==2.9.9

# ============================================
# OPTIONAL: Reinforcement Learning (DQN/PPO/A2C)
# Heavy dependency: ~500MB (PyTorch + stable-baselines3)
# Install ONLY if you want to use RL agents:
#   pip install -r requirements-rl.txt
# Then train agents:
#   python scripts/train_rl_agents.py --symbol BTCUSDT --interval 1h --limit 2000
# Enable in .env:
#   ENABLE_RL_STRATEGY=true
# ============================================

